
// Legacy file - keeping only for backward compatibility
// New implementations use dynamic types from ConfigTypes and DataTypes

// Re-export from new type system for compatibility
export type { TreemapNode } from '@/types/DataTypes';
